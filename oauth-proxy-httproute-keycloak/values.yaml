ingressHost: service.rnd.perferctlab.com
upstream: http://service.namespace/

keycloak_url: "https://auth.????????"
keycloak_group: "admin"
email_as_username: false
scope: "openid email profile groups"

clientID: ?????

secretName: ?????

additionalIngressAnnotations:

additionalPaths: []
# - path: /path
#   pathType: Prefix
#   backend:
#     service:
#       name: back
#       port:
#         name: http

gateway:
  name: gateway
  namespace: istio-system

resources:
  requests:
    memory: "64Mi"
    cpu: "50m"
  limits:
    memory: "64Mi"
    cpu: "200m"

nodeSelector: {}

replicaCount: 1

autoscaling:
  enabled: false
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70
  # targetMemoryUtilizationPercentage: 80

image:
  repository: cr-cache.companion.dev.perfectart.ai/bitnami
  name: oauth2-proxy
  tag: 7.1.2-debian-10-r3
  pullPolicy: Always
  pullSecrets:
    - regcred
